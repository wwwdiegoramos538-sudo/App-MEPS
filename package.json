{
  "name": "meps-platform",
  "version": "1.0.0",
  "description": "MEPS - Traduciendo el Futuro | Plataforma SaaS de traduccion con IA",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run install:all",
    "dev": "concurrently -n db,backend,frontend -c green,blue,cyan \"npm run db:start --prefix backend\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "db:generate": "npm run db:generate --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:push": "npm run db:push --prefix backend",
    "db:studio": "npm run db:studio --prefix backend",
    "db:setup": "npm run db:setup --prefix backend",
    "db:start": "npm run db:start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
